# Admin Module

Planned owner for moderation queues, reports, disputes, and admin decisions.

When implemented, start with an admin controller, focused service, DTOs, tests,
and this README. Enforce admin authorization in services as well as route guards.
Admin may call exported module services but must not write other modules' tables.

# Server Documentation Archive

The duplicated BMAD snapshot was removed from this repository after the shared
product contract moved to the sibling `sharek-docs` repository. Its prior
contents remain recoverable through this repository's Git history.

Selected historical artifacts are retained under `../../../docs/archive/` in the
local workspace.
